# ContentItem Property

## Notes

The ContentItemType enumeration may take the following values:

This property is currently used only when importing MS Word-supported documents using the MSOffice read module.

While the Content setting does allow the import of comments (popup or postit note type structures) they are non-interactive. If this is something you require you may prefer the RichConversion property.

## Example

None.

