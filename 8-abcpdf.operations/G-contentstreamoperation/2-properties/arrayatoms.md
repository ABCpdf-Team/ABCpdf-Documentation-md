# ArrayAtoms Property

## Notes

The content streams for the items that have been found.

These are stored as a dictionary mapping the owning object with the content stream.

The content stream itself is represented as a series of graphics operators converted using the ArrayAtom.FromContentStream method.

## Example

None.

